[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-config"
version = "0.1.0"
description = "PLATO configuration — agent identity, room settings, fleet parameters"
license = {text = "MIT"}
requires-python = ">=3.8"
